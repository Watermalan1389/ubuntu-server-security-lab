Host System:

OS: Ubuntu Linux Virtualization: Virtual Machine Manager (KVM/QEMU) Virtual Machines:

Ubuntu Server (Target)

2 CPU cores 4 GB RAM 25 GB storage Kali Linux (Attacker)

Used to simulate attacks such as SSH brute-force attempts. Network:

Both machines are connected to an isolated virtual network (192.168.100.0/24).
