Host System:
- OS: Ubuntu Linux
- Virtualization: Virtual Machine Manager (KVM/QEMU)

Virtual Machines:

Ubuntu Server (Target)
- 4 CPU cores
- 8 GB RAM
- 100 GB storage

Kali Linux (Attacker)
- Used to simulate attacks such as SSH brute-force attempts.

Network:
- Both machines are connected to an isolated virtual network (192.168.100.0/24).
